name: SAST

on:
  pull_request: {}
  push:
    branches:
    - master
    - main
    - next/*
    - release/*
  workflow_dispatch: {}


jobs:
  semgrep:
    name: Semgrep SAST
    runs-on: ubuntu-latest
    permissions:
      # required for all workflows
      security-events: write
      # only required for workflows in private repositories
      actions: read
      contents: read

    if: (github.actor != 'dependabot[bot]')

    steps:
      - uses: actions/checkout@v3
      - uses: Kong/public-shared-actions/security-actions/semgrep@d4d6b2a7e202398f62eb37c554df9732b27d9d84
