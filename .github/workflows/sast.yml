name: SAST

on:
  pull_request: {}
  push:
    branches:
    - master
    - main
    - next/*
    - release/*
  workflow_dispatch: {}


jobs:
  semgrep:
    name: Semgrep SAST
    runs-on: ubuntu-latest
    permissions:
      # required for all workflows
      security-events: write
      # only required for workflows in private repositories
      actions: read
      contents: read

    if: (github.actor != 'dependabot[bot]')

    steps:
      - uses: actions/checkout@v3
      - uses: Kong/public-shared-actions/security-actions/semgrep@62643b74f79f6a697b9add1a2f9c069bf9ca1250
