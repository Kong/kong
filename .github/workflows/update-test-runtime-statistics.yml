name: Update test runtime statistics file for test scheduling
on:
  workflow_dispatch:
  schedule:
    - cron: "1 0 * * SAT"
  # push rule below needed for testing only
  push:
    branches:
      - feat/test-run-scheduler-ee

jobs:
  process-statistics:
    name: Download statistics from GitHub and combine them
    runs-on: ubuntu-latest-kong
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}

      - name: Process statistics
        uses: Kong/gateway-test-scheduler/analyze@1ca1120856ffaf9ba87e14564ccabe91e2b4ebf3 # v5
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}
        with:
          workflow-name: build_and_test.yml
          test-file-runtime-file: .ci/runtimes-ee.json
          artifact-name-regexp: "^test-runtime-statistics-\\d+$"

      - name: Upload new runtimes file
        uses: Kong/gh-storage/upload@v1
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}
        with:
          repo-path: Kong/gateway-action-storage/main/.ci/runtimes-ee.json
