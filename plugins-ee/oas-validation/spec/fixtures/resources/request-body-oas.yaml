openapi: "3.0.0"
info:
  title: Sample API
  description: A Sample OpenAPI Spec
  version: 1.0.0
paths:
  /notify:
    post:
      responses:
        200:
          description: OK

  /notify-with-body:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                key:
                  type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
