swagger: "2.0"
info:
  title: Sample API
  description: A Sample OpenAPI Spec
  version: 1.0.0

paths:
  /ref-case-1:
    post:
      description: ''
      parameters:
        - in: "body"
          name: body
          required: true
          schema:
            $ref: "#/definitions/Simple"
      responses:
        '200':
          description: OK

  /ref-case-2:
    post:
      description: ''
      parameters:
        - in: "body"
          name: body
          required: true
          schema:
            type: array
            items:
              $ref: "#/definitions/Simple"
      responses:
        '200':
          description: OK

  /ref-case-3:
    post:
      description: ''
      parameters:
        - in: "body"
          name: body
          required: true
          schema:
            type: object
            properties:
              key_integer:
                type: integer
              key_boolean:
                type: boolean
              simple:
                $ref: "#/definitions/Simple"
      responses:
        '200':
          description: OK

  /recursive-ref-request-case-1:
    post:
      description: ''
      parameters:
        - in: "body"
          name: body
          required: true
          schema:
            $ref: "#/definitions/ComparedTcmAgreementsDto"
      responses:
        '200':
          description: OK

  /recursive-ref-response-case-1:
    post:
      description: ''
      responses:
        '200':
          description: OK
          schema:
            $ref: "#/definitions/ComparedTcmAgreementsDto"

definitions:
  Simple:
    type: object
    properties:
      key_string:
        type: string
  ComparedTcmAgreementsDto:
    type: object
    properties:
      differentFields:
        type: array
        items:
          type: string
      securityEligibilityRules:
        $ref: "#/definitions/ConstraintTree"
  ConstraintTree:
    type: object
    properties:
      constraint:
        type: string
      leaves:
        type: array
        items:
          $ref: "#/definitions/ConstraintTree"
      operation:
        type: string
        enum:
          - IS
          - AND
          - OR
