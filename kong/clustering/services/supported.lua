-- DP and CP shares one list.
-- Always from newer version to older version.

return {
    config_sync = {
        {version = "v1", description = "The configuration synchronizing service. (JSON and Gzip)"},
    },
}