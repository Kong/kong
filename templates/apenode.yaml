---

# Apenode version
version: 0.1

# Specify the DAO to use
dao:
  factory: sqlite
  properties:
    memory: false
    file_path: /tmp/apenode
  #factory: cassandra
  #properties:
  #  host: "127.0.0.1"
  #  port: 9042
  #  timeout: 1000
  #  keyspace: apenode
  #  keepalive: 60000

# Enabled Plugins, in this order
plugins_enabled:
  - authentication
  - ratelimiting
  - networklog

---
