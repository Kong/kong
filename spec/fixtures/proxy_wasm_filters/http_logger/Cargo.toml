[package]
name = "http_logger"
version = "0.1.0"
authors = ["Kong"]
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "src/filter.rs"

[dependencies]
proxy-wasm = "0.2.0"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
