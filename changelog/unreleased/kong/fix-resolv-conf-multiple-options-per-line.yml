message: >
  Fixed an issue where the resolv.conf parser silently dropped all options
  when multiple options were specified on a single `options` line
  (e.g. `options ndots:2 timeout:2 attempts:3`, as generated by Kubernetes
  dnsConfig). All whitespace-separated options on the line are now parsed.
type: "bugfix"
scope: "Core"
